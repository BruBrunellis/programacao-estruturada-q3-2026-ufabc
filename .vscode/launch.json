{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C: depurar arquivo ativo (GDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.out",
      "windows": {"program": "${fileDirname}/${fileBasenameNoExtension}.exe"},
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "C: compilar arquivo ativo"
    }
  ]
}
